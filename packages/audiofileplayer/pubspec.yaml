name: audiofileplayer
description: A Flutter plugin for audio playback.
version: 1.0.0
author: Daniel Iglesia <diglesia@google.com>
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  logging:
  meta:
  uuid:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: com.google.flutter.plugins.audiofileplayer
    pluginClass: AudiofileplayerPlugin
